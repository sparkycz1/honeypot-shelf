"""Single source of truth for the running app version, shown in the UI
footer and `GET /api/v1/settings`. Bump alongside `version` in
`pyproject.toml` — see CLAUDE.md's "Commands" section."""

APP_VERSION = "0.1.0"
